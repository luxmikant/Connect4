# Production Environment Variables for Connect4 Frontend
# Copy this to web/.env.production and update with your actual URLs

# Backend API URL (update after deploying to Render)
VITE_API_URL=https://connect4-server.onrender.com

# WebSocket URL (update after deploying to Render)
# Must use wss:// for secure WebSocket connection
VITE_WS_URL=wss://connect4-server.onrender.com/ws

# Optional: Analytics/Tracking
# VITE_ANALYTICS_ID=your_analytics_id
