# Environment Configuration Example
# Copy this file to .env and update with your actual values
# See docs/cloud-setup-guide.md for detailed setup instructions

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Application Environment (development, staging, production)
ENVIRONMENT=development

# =============================================================================
# SERVER CONFIGURATION
# =============================================================================

# Server port and host
SERVER_PORT=8080
SERVER_HOST=0.0.0.0

# CORS origins (comma-separated list)
# Development: include localhost origins
# Production: include your actual frontend domain
SERVER_CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Server timeouts (seconds)
SERVER_READ_TIMEOUT=30
SERVER_WRITE_TIMEOUT=30

# =============================================================================
# SUPABASE DATABASE CONFIGURATION
# =============================================================================

# Supabase PostgreSQL connection string
# Format: postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres?sslmode=require
# 
# To get your connection string:
# 1. Go to your Supabase project dashboard
# 2. Settings → Database → Connection string → URI
# 3. Replace [YOUR-PASSWORD] with your database password
#
# Local development (using docker-compose):
DATABASE_URL=postgres://postgres:password@localhost:5432/connect4?sslmode=disable
#
# Supabase cloud (replace with your actual values):
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT_REF.supabase.co:5432/postgres?sslmode=require

# Database connection pool settings
DATABASE_MAX_OPEN_CONNS=25
DATABASE_MAX_IDLE_CONNS=5
DATABASE_CONN_MAX_LIFETIME=300

# =============================================================================
# CONFLUENT CLOUD KAFKA CONFIGURATION
# =============================================================================

# Kafka bootstrap servers
# Local development (using docker-compose):
KAFKA_BOOTSTRAP_SERVERS=localhost:9092
#
# Confluent Cloud (replace with your actual cluster):
# KAFKA_BOOTSTRAP_SERVERS=pkc-xxxxx.region.provider.confluent.cloud:9092

# Confluent Cloud API credentials
# Get these from: Cluster Overview → API Keys → Create key
# Leave empty for local development
KAFKA_API_KEY=
KAFKA_API_SECRET=

# Kafka topic and consumer group
KAFKA_TOPIC=game-events
KAFKA_CONSUMER_GROUP=analytics-service

# =============================================================================
# REDIS CONFIGURATION
# =============================================================================

# Redis connection
# Local development:
REDIS_URL=localhost:6379
REDIS_PASSWORD=
REDIS_DB=0
#
# Redis Cloud (if using):
# REDIS_URL=redis://username:password@redis-host:port
# REDIS_PASSWORD=your-redis-password

# =============================================================================
# PRODUCTION ENVIRONMENT VARIABLES
# =============================================================================
# 
# For production deployment, set these environment variables:
#
# Required:
# ENVIRONMENT=production
# DATABASE_URL=postgresql://postgres:PASSWORD@db.PROJECT-REF.supabase.co:5432/postgres?sslmode=require
# KAFKA_BOOTSTRAP_SERVERS=pkc-xxxxx.region.provider.confluent.cloud:9092
# KAFKA_API_KEY=your-confluent-api-key
# KAFKA_API_SECRET=your-confluent-api-secret
# SERVER_CORS_ORIGINS=https://your-frontend-domain.com
#
# Optional (with defaults):
# SERVER_PORT=8080
# DATABASE_MAX_OPEN_CONNS=50
# DATABASE_MAX_IDLE_CONNS=10
# KAFKA_TOPIC=game-events
# KAFKA_CONSUMER_GROUP=analytics-service

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================
#
# 1. Never commit the .env file to version control
# 2. Use strong passwords for production
# 3. Rotate API keys regularly
# 4. Monitor usage to stay within free tier limits
# 5. See docs/cloud-setup-guide.md for detailed setup instructions